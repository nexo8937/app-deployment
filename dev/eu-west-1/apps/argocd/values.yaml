global:
  domain: ""
server:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
    hosts:
      - host: "*"   
    paths:
      - path: /argocd
        pathType: Prefix
  extraArgs:
    - --insecure
